chromadb==0.6.3
Crawl4AI==0.4.248
langchain_community==0.4.1
langchain_text_splitters==1.1.1
Requests==2.32.5
streamlit==1.42.0

# Fallback fetcher layer (fetcher.py) - used when crawl4ai returns empty
# content from JS-heavy or anti-bot pages. httpx fetches the raw HTML,
# trafilatura extracts the main article text, BeautifulSoup is the final
# naive p-tag scrape if trafilatura can't find an article.
httpx==0.27.2
trafilatura==1.12.2
beautifulsoup4==4.12.3

requests==2.31.0
urllib3==2.0.7
charset-normalizer==3.3.2